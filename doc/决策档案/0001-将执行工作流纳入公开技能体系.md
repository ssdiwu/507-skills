# 将执行工作流纳入公开技能体系

当前代码工作流把实现环节交给 Pi 专属的 prompt（提示词），导致同一套方法无法被 Codex 等其他 Agent（智能体）平台直接复用。项目决定将这些执行工作流正式纳入公开的 `507-skills` 技能体系，而不是另建执行仓库或仅保留个人扩展层；这样可以用同一安装入口和唯一事实源贯通“设计—执行—验收”，代价是仓库需要承担执行技能的公开边界、跨平台兼容与持续维护责任。
