# 共享技能保持宿主中立

公开 skill（技能）只定义触发条件、动作边界、修改权限、产物与验收标准，不写特定产品的条件分支，也不预建宿主 adapter（适配器）。技能发现、工具权限、显式调用和平台专属调度留在各宿主自己的配置层；不同兼容宿主应保持用户可观察的结果与边界一致，具体工具过程可以不同。某个宿主是否支持基于 description（描述）的自动发现、显式调用或递归扫描，以该宿主实现为准，不作为公开 skill 的能力承诺。
