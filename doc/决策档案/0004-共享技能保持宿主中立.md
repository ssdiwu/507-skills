# 共享技能保持宿主中立

公开 skill（技能）只定义触发条件、动作边界、修改权限、产物与验收标准，不写 Pi/Codex 条件分支，也不预建宿主 adapter（适配器）；`dgoal`、`dteam` 等平台专属调度留在各宿主的 `AGENTS.md`。Pi 与 Codex 必须保持用户可观察的结果与边界一致，具体工具过程可以不同；两端同时支持基于 description（描述）的自然语言触发和宿主原生显式调用。
