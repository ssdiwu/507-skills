# inspection 与 simplify 采用双层证据链

`507-inspection` 保持“监理不施工”，负责发现架构摩擦、提供可定位的观察证据与置信等级，并把整份候选报告交给 `simplify`。`simplify` 为每项候选建立行为基线或验证信号：证据成立才实施，不成立则带证据关闭，因此“处理完整报告”不等于强制修改每个候选；它也可直接处理指定范围。所有改造可深入内部模块、抽象与接缝，但必须保持公开契约和调用者可观察行为不变，需要改变产品行为的候选转入需求与正常实施流程。
